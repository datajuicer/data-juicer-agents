# DJX Frontend (M1)

## Run local API

```bash
uv run djx-ui-api --host 127.0.0.1 --port 8787 --reload
```

## Run frontend

```bash
npm install
npm run dev
```

Open `http://127.0.0.1:5173`.

The settings page writes to `./.djx/config.json` in the project directory.
